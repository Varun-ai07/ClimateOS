# ClimateNarrative Frontend

## Setup
1. Install dependencies: `npm install`
2. Create env: `cp .env.example .env`
3. Start dev server: `npm run dev`
4. Build: `npm run build`

Default backend: `http://localhost:8000`
